data:
  jet_types: [g]
  num_particles: 30

inference:
  n_samples: 10000
  integration_steps: 64
  batch_size: 256
  seed: 42
  use_cfg: false
  cfg_guidance_weight: 2.0
  sampler: euler
  vf_mode: none
  prior_dist: axis_aligned
